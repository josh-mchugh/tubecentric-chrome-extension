stages:
  - build

build:
  image: node:latest
  stage: build
  before_script: 
    - npm ci
    - npm install -g gulp
  script: 
    - gulp init
    - npm run build:popover
  artifacts:
    paths:
      - package/*.zip